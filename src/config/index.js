import requestConfig from './request-config'
import requestUrl from './request-url'
import responseConfig from './response-config'

// 项目配置项汇总
// 获取配置项示例：import { ? } from '@/config'
export {
  requestConfig,
  requestUrl,
  responseConfig
}